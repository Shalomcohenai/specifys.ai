# Archived files

- **admin-dashboard.js** – Legacy admin UI (replaced by `pages/new-admin-dashboard.html` and `assets/js/new-admin-dashboard/`). Not loaded by any page.
- **admin.js** – Bundle that imported admin-dashboard.js. Not referenced in any HTML.

Archived as part of architecture cleanup (plan: keep current admin = new-admin-dashboard; remove unused code).
